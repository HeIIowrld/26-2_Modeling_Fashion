# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

두 목적을 동시에 가진 사용자층 (사용자 확인, "둘 다"):
- **실사용자**: 출근·데이트·여행 등 실제 코디 고민이 있어 전신사진을 넣고 추천을 받는 일반인.
- **DSL 26-2 과제 심사·시연 대상**: 팀원·강사 등. FITTA는 DSL 2026-2 모델링 과제 산출물이며, 데모 겸 실사용 프로토타입으로 설계됐다.

## Product Purpose

전신사진 한 장으로 체형과 지금 입은 옷을 분석해, 패션 규칙에 맞는 상품을 근거와 함께 추천하고, 원하면 실제로 입혀본 합성 사진(가상 피팅)까지 만들어준다. 성공은 사용자가 "왜 이 옷을 추천했는지" 납득할 근거를 얻는 것과, 원할 때 입은 모습을 미리 확인하는 것.

파이프라인: 사진 → 체형·착장 분석 → 규칙 기반 추천 → 가상 피팅.

## Positioning

사용자 확인: 다른 코디 추천 앱은 보통 추천만 하고 입혀보진 않는다는 점에서, FITTA는 **① 규칙 기반 추천 근거 제시**와 **② 실제 가상 피팅 합성**을 함께 제공하는 것이 핵심 차별점.

- 근거: `FASHION_RULES_MASTER.md`의 R-* 규칙(50개 중 43개 구현)으로 후보를 거르고 점수를 매기며, 왜 추천했는지 근거를 함께 낸다.
- 합성: CatVTON으로 추천한 옷을 실제로 입힌 사진을 만든다.

## Operating Context

웹 화면은 4단계 흐름: **사진**(코디 판단용 필수, 체형 파악용 사진은 선택) → **조건**(코디 목적/원하는 스타일/변경 범위/계절/예산/유지하고 싶은 옷) → **분석** → **결과**.

실행: 저장소 루트에서 `python web/run_web.py` (FastAPI + uvicorn). `--check`(점검만), `--port`, `--lan`, `--no-browser` 옵션 지원. 같은 분석·추천 모듈을 `ai_fashion_recommender/main.ipynb` 노트북에서도 순서대로 호출해 쓸 수 있다.

업로드한 사진은 저장소나 클라우드에 남기지 않는다 — OS 임시 폴더(`%TEMP%/fitta_web_sessions/`)에 저장 후 30분 뒤 자동 삭제. 프로젝트가 OneDrive 동기화 폴더 안에 있어도 전신사진이 클라우드로 올라가지 않게 하려는 의도적 설계.

## Capabilities and Constraints

- **체형 분석**: MediaPipe Pose로 어깨·골반·무릎·발목을 찾아 비율을 계산. 사진만으로 실제 신체 치수를 잴 수 없으므로 절대값이 아니라 참고값으로만 쓴다 (UI에도 명시).
- **착장 분석**: FASHN Human Parser(픽셀 단위 분할) + FashionSigLIP 위에 얹은 학습된 17개(문서 표기 기준) 속성 헤드로 종류·소매·기장·핏·패턴·소재를 읽는다.
- **가상 피팅(CatVTON)**: 구현되어 있으나 기본값 `ENABLE_VTON=False`. 로컬 CPU 환경에서는 비실용적이며, GPU 서버(dsl01)에서만 실용적 속도(20 steps/약 5.8초, 768×1024). 첫 실행 시 HuggingFace에서 체크포인트 약 5GB 다운로드 필요. **CatVTON 가중치는 CC BY-NC-SA 4.0(비상업) 라이선스** — 상업적 배포에는 쓸 수 없다.
- **의도된 설계, 버그 아님**: 키·체중은 추천에 반영되지 않는다(상품 실측 사이즈 데이터 부족으로 규칙 R-SIL-02 미구현). 기본 설정(변경 범위=전체 + 체형 반영 안 함)에서는 업로드한 사진이 추천 결과에 영향을 주지 않는다(R-KOR-02). 둘 다 규칙 전제가 바뀌기 전까지는 고칠 대상이 아니다.
- **미검증/측정 불가 항목** (성능이 나쁜 것과 구분할 것): 가디건·니트·베스트는 학습 데이터는 충분해도 검증 표본이 1/4/2개뿐이라 개선 여부 자체를 측정할 수 없다. 알려진 혼동 방향: 가디건→코트, 후드티→탑, 니트→티셔츠, 청바지→팬츠. 사진 기반 둘레 추정은 줄자 실측과 비교 검증된 적 없다. 체형 판정 경계표(`data/body_shape_reference.json`)는 한국인 표본이 아니다.
- **재학습 금지**: 배포된 속성 모델(22,341 crop 학습)은 Fashionpedia train split이 로컬에 없어 지금 재학습하면 오히려 성능이 나빠진다.
- SMPL 계열은 쓰지 않는다(가입 필요 + 비상업 라이선스) — MediaPipe 분할로 대체.
- 패션 규칙은 문헌 조사 기반이며 전문가 합의나 사용자 실험을 거치지 않았다.
- 속성 헤드는 공개 데이터(Fashionpedia, Fashion200K)로 학습해, 한국 사용자 전신사진에서의 성능은 별도 검증이 필요하다.

## Brand Commitments

브랜드명 **FITTA**. 웹 UI는 한국어 단일 언어.

## Evidence on Hand

- 상품 카탈로그: `ai_fashion_recommender/data/products.csv` (무신사 등에서 수집한 대표컷 기반 크롤링 원본 — 그대로 쓰면 규칙이 조용히 무력화될 수 있어 파이프라인을 거쳐야 함).
- 사람 사진 데이터셋: `datasets/people/{men,women}`(남 50·여 52장, 인스타그램 수집) — 연구·검증용, 재배포 금지, 저작권상 커밋되지 않음.
- 테스트 스위트: `ai_fashion_recommender/tests/` — 2026-08-21 기준 187개 전부 통과 확인.
- 실사용자 후기·케이스 스터디·벤치마크 수치는 없음. 만들어내지 말 것.

## Product Principles

- 추천은 항상 근거(적용된 규칙)와 함께 제시한다 — 블랙박스로 결과만 보여주지 않는다.
- 측정하지 못한 값과 측정했지만 나쁜 값을 UI에서도 구분한다. 신체 치수·둘레처럼 검증되지 않은 값을 절대값처럼 보여주지 않는다.
- 사용자 사진은 최소 보관·자동 삭제 — 클라우드 동기화 폴더 안에서 실행되더라도 원본 사진이 밖으로 나가지 않게 한다.
- 비상업 라이선스(CatVTON) 제약을 넘어서는 기능·배포 약속을 하지 않는다.
- 데모(과제 심사용 설명력)와 실사용(코디 고민 해결) 두 목적을 함께 만족해야 한다 — 어느 한쪽만을 위해 다른 쪽을 희생하지 않는다.
