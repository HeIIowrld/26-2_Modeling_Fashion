# 상의·하의·신발 가상 피팅 확장

작성: 2026-09-19. **구현·코드 검증에 더해 실제 GPU 생성 77건(점검 5건 + 사진 6장 × 신발 4개를 프롬프트 3종으로 24건씩)까지 확인했다. 코드 기본값은 비활성이며, 운영 서버에서는 모델 경로를 지정해 신발 생성을 활성화했다. 최신 배포 상태는 문서 마지막 절을 참고한다.**

## 기존 코드에서 달라진 점

GitHub `origin/main`의 `39369de`까지 확인했다.

- PR #24: 신발 인식 헤드, 신발 추천·검색, 퍼스널 톤, 성별 입력, LLM 추천 관련 변경이 추가됐다.
- PR #25: 상품별 추천 이유와 근거 표시가 추가됐다.
- 추천에는 신발이 들어왔지만 생성은 상의·하의 두 종류만 처리했다. 화면에서도 신발 입어보기가 막혀 있었다.
- 이전 VTON 품질 개선 PR #23은 아직 병합되지 않았다. 이번 작업 브랜치는 최신 main과 해당 개선을 합쳐 기존 마스크·기장·품질 검사를 유지한다. 원래 작업 폴더의 수정사항과 가중치는 그대로 두었다.

## 생성 방식

1. 무신사에서 선택한 상의·하의의 실제 상품 이미지로 기존 CatVTON 합성을 실행한다.
2. 합성된 사진을 다시 파싱해 발과 신발의 위치를 찾는다.
3. 사진 하단 크롭, 발 마스크, 선택한 신발의 **실제 이미지**를 FLUX.2-klein-4B의 참조 이미지 인페인팅에 넣는다.
4. 생성 결과 중 발 마스크 안쪽만 원본에 합친다. 옷·얼굴·배경의 마스크 밖 픽셀은 이 단계에서 보존한다. JPEG 저장 시 압축 차이는 생길 수 있다.

상의만, 하의만, 신발만 또는 세 종류의 조합을 선택할 수 있다. 신발 단독 선택은 CatVTON을 실행하지 않는다. 상의·하의 단독 요청은 기존 어댑터로 그대로 전달한다.

양쪽 발목·발끝의 위치와 가시성을 먼저 검사한다. 발이 사진 밖에 있거나 가려졌으면 신발 합성을 막는다. 바지·치마 영역은 신발 마스크에서 제외한다. 신발 단계가 실패하면 중간의 옷 합성본을 완성본 캐시에 저장하지 않는다.

웹의 상품 선택, 서버 요청 한도, 카테고리별 조합 생성, 결과 캐시에 신발을 연결했다. 신발 모델이 준비되지 않았으면 상의·하의만 활성화된다.

## 모델 선택 근거

| 후보 | 확인 사항 | 결정 |
| --- | --- | --- |
| [FastFit 공식 GitHub](https://github.com/Zheng-Chong/FastFit), [HF 모델](https://huggingface.co/zhengchong/FastFit-MR-1024) | 여러 상품을 한 번에 입히는 공식 코드와 가중치가 있다. [상세 라이선스](https://github.com/Zheng-Chong/FastFit/blob/main/LICENSE.md)에 비운영·최종 사용자 직접 상호작용 제한이 있다. | 서비스 연결 후보에서 제외. 가중치를 사용하지 않았다. |
| [DressCode-MR 데이터](https://huggingface.co/datasets/zhengchong/DressCode-MR) | 관련 다중 참조 연구 데이터지만 접근 승인이 필요한 연구용 데이터다. HF Spaces의 제3자 데모 존재와 공식 구현 여부를 구분했다. | 승인 신청·데이터 사용·외부 데모 업로드 없음. |
| [Qwen-Image-Edit-2511](https://huggingface.co/Qwen/Qwen-Image-Edit-2511) | 다중 이미지 편집 모델. 이번 신발 단계보다 큰 모델을 추가할 필요성이 낮다. | 이번 구현에는 사용하지 않음. |
| [FLUX.2-klein-4B](https://huggingface.co/black-forest-labs/FLUX.2-klein-4B), [공식 코드](https://github.com/black-forest-labs/flux2) | Apache-2.0, 공개 체크포인트, 참조 이미지 편집 지원. 모델 카드의 약 13GB VRAM 안내는 공급자 수치이며 우리 조합의 실측값은 아니다. | 신발 국소 편집에 도입. 기존 CatVTON의 별도 비상업 라이선스는 여전히 적용된다. |

사용 리비전: `e7b7dc27f91deacad38e78976d1f2b499d76a294`.
서버에 약 15GB 체크포인트 다운로드를 완료했다. 별도 학습은 하지 않았다.
하단 크롭의 긴 변 768, 4 steps, guidance 1, seed 42, 모델 CPU offload 설정이다.
설치된 diffusers 0.40.0의 `Flux2KleinInpaintPipeline`과 `image_reference` 인자를 확인했다. Transformers 5.15.1, Accelerate 1.14.0 환경이다. 기존 환경에 무조건 의존성을 업그레이드하지 않고 격리 환경에서 검증한다.

## 검증 상태

- 전체 로컬 검사: 472개 통과, 세부 검사 662건 통과. 이후 신발 전용 테스트 3건을 추가해 해당 파일 12개 모두 통과했다.
- JavaScript 구문 검사 및 diff 공백 검사 통과.
- 실제 상품 이미지 전달, 바지 보호, 마스크 밖 픽셀 보존, 가려진 발 차단, 신발 단독/의류 단독 분기, 실패 시 완성본 미저장, 세 카테고리 캐시와 상품별 조합을 검사했다.
- 최신 팀 코드의 `predict_shoes` 인터페이스에 기존 테스트용 분류기 모형을 맞췄다. 기장 보류 테스트의 판정 기준은 유지했다.
- 서버 CPU 작업 `231030` 완료: FASHN + MediaPipe + 실제 인페인팅 클래스 import 확인. `049713_0.jpg`, `Simon_1.png`의 발 마스크를 직접 확인했다. `1-model_3.png`, `2-model_4.png`는 발 가시성 부족으로 차단됐다.
- GPU 실행 5건 완료(사진 2장 × 신발 2개 + 상의·하의·신발 전체 조합 1건, 오류 0건). 노드의 GPU 5장이 모두 차 있어 대기하던 작업 `231029` 대신, 유휴 상태인 운영 작업 할당 안에서 `srun --overlap`으로 실행했다. 운영 서비스는 내리지 않았고 요청 처리에도 영향이 없었다.

| 항목 | 결과 |
| --- | --- |
| 신발 1건 생성 시간 | 11~15초(첫 건은 모델 적재 포함 287초) |
| 상의·하의·신발 3종 | 42초 |
| 최대 VRAM | 8.8GB (모델 카드 안내 13GB보다 낮음, CPU 오프로드 설정 기준) |
| 마스크 밖 픽셀 보존 | 5건 모두 `true` |
| 변경 비율 | 0.58~0.72 (무변화 실패 기준 0.02 통과) |
| 의류 품질 검사 | 상의·하의 각 1회 시도로 실패 없음, 검사 시간 0.78초·0.26초 |
| 발 미관측 차단 | `1-model_3`, `2-model_4` 2장 차단 유지 |
- 입력 신발은 무신사 공개 상품 검색에서 가져온 [흰색 스니커즈](https://www.musinsa.com/products/2897209)와 [검은색 로퍼](https://www.musinsa.com/products/7024587)다. 상품 사진을 저장소에 커밋하거나 재배포하지 않는다.

이 2장은 기존 평가 사진을 재사용한 실행 점검용이다. 독립된 사람에 대한 일반화 평가 또는 사용자 사진 검증이라고 주장하지 않는다.

## 눈으로 본 결과

5건을 원본·결과·발 확대·상품 사진으로 나란히 놓고 확인했다(자동 시각 검토, 독립 사람 라벨이 아니다).

- 흰색 스니커즈: 두 사람 모두 양발이 상품과 같은 흰 가죽 로우탑으로 바뀌고 로고 위치·빨간 포인트·밑창 모양이 상품과 닮았다. 발 방향과 바닥 그림자가 유지된다.
- 검정 플랫폼 로퍼: 페니 스트랩과 두꺼운 밑창이 재현됐다. 양말과 바지 밑단이 그대로 남았다.
- 전체 조합: 상의·하의를 바꾼 뒤 신발까지 교체돼도 청바지 밑단이 신발을 자연스럽게 덮었다.
- 아쉬운 점: 스니커즈 밑창·앞코에 때가 탄 듯한 질감이 들어가는 경우가 있다. 상품 사진은 새 제품이다.

이 5건의 검증 범위는 스튜디오 정면 사진 2장, 신발 2개, 시드 42다. 아래의 확대 검증으로 사진 6장·신발 4개까지 넓혔고, 자세는 여전히 정면 서 있는 사진뿐이다.

## 확대 검증 24건

DeepFashion-MultiModal 표본에서 하의 기장·차림이 서로 다른 사진 6장을 뽑고 신발 4개를 모두 조합해 24건을 생성했다(사진 7장 중 1장은 발이 보이지 않아 차단됐다). 신발은 무신사 공개 상품 검색에서 가져온 [흰 스니커즈](https://www.musinsa.com/products/2897209), [검정 플랫폼 로퍼](https://www.musinsa.com/products/7024587), [흰 스케이트화](https://www.musinsa.com/products/5046628), [검정 스웨이드 더비](https://www.musinsa.com/products/6886576)다. 사진과 표본은 평가 폴더에만 두고 저장소에 커밋하지 않는다(DeepFashion-MultiModal은 비상업 연구 전용).

| 항목 | 결과 |
| --- | --- |
| 생성 성공 | 24/24, 오류 0건 |
| 1건 생성 시간 | 중앙값 11.0초(첫 건은 모델 적재 포함 156초) |
| 최대 VRAM | 8.5GB |
| 마스크 밖 픽셀 보존 | 24건 모두 `true` |
| 변경 비율 | 0.48~0.73 |

반복되는 결함 두 가지를 확인했다(자동 시각 검토).

- **새 상품인데 때가 탄 질감**: 24건 중 20건에서 밑창·앞코에 얼룩이나 스침 자국이 들어갔다. 상품 사진은 모두 새 제품이다.
- **없던 발목 착용물 생성**: 원본이 맨발목인데 양말이 생기거나(`MEN-Shorts-id_00000337`), 원래 하이탑 발목 부분이 접힌 천처럼 남는 경우(`MEN-Shorts-id_00004620`)가 14건이었다. 원본이 하이탑·부츠라 발목을 가리고 있던 사진에서는 무언가를 채워야 하므로 전부를 결함으로 보기는 어렵다.

## 프롬프트 A/B 24쌍 × 3종

결함 두 가지를 프롬프트로 줄일 수 있는지 **같은 사람·같은 신발·같은 시드**로 24쌍씩 세 번 생성해 비교했다.

| | v1(원래) | v2(청결 + 양말 금지) | v3(청결만, 채택) |
| --- | --- | --- | --- |
| 때 탄 질감(육안) | 20/24 | 0/24 | 3/24 |
| 없던 발목 착용물(육안) | 14/24 | 17/24 | 14/24 |
| 얼룩 대리 지표 평균 | 0.272 | 0.245 | 0.236 |
| v1보다 깨끗한 흰 신발 사례 | – | 10/12 | 10/12 |

- v2는 밑창을 가장 깨끗하게 만들었지만 **맨발목인 사람에게 없던 양말을 새로 그려 넣었다**(`MEN-Shirts_Polos-id_00000925` 3건). 프롬프트에서 "socks"를 유지 목록과 금지 문구에 세 번 언급한 것이 역효과로 보인다.
- v3은 v2에서 양말 관련 문장을 모두 빼고 청결 문장만 남긴 것이다. 때 탄 질감은 거의 사라지고 발목 착용물은 v1 수준으로 되돌아왔다. **v3을 `shoe_tryon.PROMPT`로 채택했다.**
- 세 실행 모두 24/24 성공, 오류 0건, 마스크 밖 픽셀 보존 24/24다. 생성 시간 중앙값은 11.0 / 9.3 / 8.9초, 변경 비율은 0.48~0.73 / 0.46~0.73 / 0.44~0.73으로 프롬프트에 따른 차이는 없었다.
- 대리 지표는 마스크 안에서 채도가 낮고 밝기가 중간인 화소 비율이다. 그림자·바닥도 함께 걸리므로 절댓값이 아니라 같은 사례끼리의 방향만 본다. 육안 판정을 대신하지 않는다.

### 기각한 가설: 마스크가 맨발목까지 덮어서 양말이 생긴다

마스크 경계를 결과 위에 겹쳐 확인한 결과 **사실이 아니었다**. 편집 영역의 위쪽 끝은 파싱이 찾은 발 영역보다 13px 위일 뿐이고, 마스크 안에서 다리 살 라벨은 전체의 4~5%다. 양말은 모델이 신발을 조금 낮게 그린 뒤 원래 신발 목선 자리를 채우면서 생긴다. 즉 마스크 크기가 아니라 생성 쪽 선택이다. 다리 쪽 팽창 축소·발목 위 컷 같은 마스크 변형은 GPU 없이 파싱 라벨 덤프(`--dump-parse`)로 미리 비교했고, 이 결함을 줄이지 못한다고 판단해 넣지 않았다.

## 남은 품질 확인

긴 부츠처럼 종아리까지 덮는 상품, 앉은 자세·측면 사진, 발이 서로 겹치는 사진은 아직 확인하지 않았다. 색이 바뀌었다는 사실만으로 상품 재현 성공을 판정하지 않는다.

현재 자동 검사는 마스크 보존과 무변화 결과 탐지까지다. 보고서의 `reference_fidelity=not_calibrated`는 상품 충실도 검사가 보정되지 않았다는 뜻이다. 기존 의류 품질 검사 정확도를 신발에 적용하지 않는다. 긴 부츠처럼 종아리까지 바꿔야 하는 상품과 복잡한 자세는 이 국소 마스크 방식의 추가 검증 대상이다.

2026-09-19 운영 배포에서 모델을 공용 경로에 별도로 보존하고 신발 생성 환경변수를 활성화했다. 서버 전용 사이즈 추천 기능과 최신 코드를 통합해 배포했다. 실사용자 사진에 대한 별도 검증은 여전히 남아 있다.

## 재현과 활성화

관련 구현: `ai_fashion_recommender/src/shoe_tryon.py`, `web/pipeline.py`, `web/app.py`, `web/static/app.js`.

모델 다운로드:

```bash
python gpu_server/scripts/prefetch_shoe_model.py --destination /path/to/flux-klein-4b
```

품질 확인 후 선택적으로 지정할 환경변수:

```bash
FASHION_SHOE_MODEL_PATH=/path/to/flux-klein-4b
```

환경변수를 비우면 기존 상의·하의 전용 생성으로 돌아간다. 신발 모델의 구성 파일과 가중치 샤드가 모두 있어야 신발 선택이 활성화된다.

서버 평가 위치: `/data1/dsl01/eval/shoes_vton_20260919/` (계산 노드는 `/mnt/data1/...`). 운영 릴리스와 분리되어 있다.

```bash
# GPU가 비어 있을 때: 점검 5건
sbatch gpu_server/jobs/shoes_vton_smoke.sbatch

# 확대 검증 24건. SHOES_BROAD_PROMPT 를 주면 프롬프트 A/B가 된다
sbatch gpu_server/jobs/shoes_vton_broad.sbatch

# GPU가 모두 차 있고 운영 작업이 유휴일 때(서비스는 계속 실행)
srun --jobid=<fitta-web JobID> --overlap --ntasks=1 --cpus-per-task=3 --mem=20G   bash /mnt/data1/dsl01/eval/shoes_vton_20260919/run_smoke_overlap.sh
```

마스크 규칙만 바꿔 보고 싶으면 GPU를 쓰지 말고 파싱 라벨을 먼저 덤프한다. `smoke_shoe_tryon.py --prepare-only --dump-parse`는 사람별 파싱 라벨과 포즈를 저장하므로, 마스크 변형은 로컬에서 면적·겹침으로 비교할 수 있다.

입력과 마스크는 `prepared/`, 추론 결과와 보고서는 `out/`에 저장된다. 추론 실패도 `out/results.json`에 기록한다. 재실행은 `gpu_server/jobs/shoes_vton_smoke.sbatch`를 사용한다. 원본 사진과 상품 이미지는 평가 폴더에서만 보관한다.

## 2026-09-19 배포 인계 점검

- PR #26의 신발 합성과 이전 품질 개선을 main에 반영하는 작업을 이어 진행했다.
- 운영 서버에만 있던 실측 사이즈 비교·후보 검색과 최신 신발·퍼스널 톤·추천 이유 변경의 충돌을 해결했다. 배포 통합본은 별도 브랜치 `codex/shoes-release`에서 관리한다.
- 공개 게이트웨이의 기존 화면에는 성별 필드가 없어 새 API의 필수 검증과 충돌했다. `change_scope`만 보내는 구버전 요청은 기존처럼 처리하고, `change_categories`를 보내는 새 요청과 명시적으로 잘못된 성별은 기존 필수 검증을 유지한다. 사진에서 성별을 추정하거나 기본값을 임의로 넣지 않는다.
- `/api/health`에 `tryon_categories`를 추가해 모델 파일 준비에 따른 실제 지원 범위를 확인한다.
- `gpu_server/scripts/smoke_shoe_api.py`로 실제 HTTP 분석·추천·신발 배치 합성·이미지 다운로드·캐시까지 검증한다.
- 프롬프트 비교 수치는 같은 개발 사진으로 선택한 결과이며, 독립된 사용자 사진에 대한 일반화 성능 추정이 아니다.

## 2026-09-20 배포 완료 기록

**최신 실행 상태(2026-09-20 05:32 KST 확인):** 최초 운영 작업 `231188`이 8시간 제한으로 정상적인 시간 만료(`TIMEOUT`)에 도달했다. 서비스 제어기가 새 작업 `231644`를 자동 제출했으나 `Priority`로 대기 중이다. GPU 5개가 모두 할당되어 시작 시각을 알 수 없다. 이 상태에서는 공개 API가 503을 반환한다. 아래의 합성 성공 기록은 시간 만료 전 운영 검증 결과이며, 현재 무중단 서비스가 제공된다는 의미가 아니다. 다른 사용자의 작업을 취소하거나 Slurm 할당 밖에서 실행하지 않았다.

- **main 반영:** [PR #26](https://github.com/HeIIowrld/26-2_Modeling_Fashion/pull/26) 병합, `961a69b`.
- **운영 릴리스:** `/data1/dsl01/releases/fitta_20260919_shoes_v3`, `fitta_current` 링크가 이 릴리스를 가리킨다.
- **운영 소스:** [codex/shoes-release](https://github.com/HeIIowrld/26-2_Modeling_Fashion/tree/codex/shoes-release), `30ac86a`. main에 없던 팀원의 사이즈 비교·후보 검색을 보존한 통합본이다. main만 그대로 덮어쓰면 이 기능이 사라지므로 배포 시 통합 브랜치를 기준으로 대조해야 한다.
- **모델:** `/mnt/data1/dsl01/shared_models/flux2-klein-4b-e7b7dc27`. 평가 디렉터리 정리와 독립적으로 모델이 남도록 공용 경로에 보존했다.
- **영구 설정:** 사용자 systemd의 `fitta-web.service.d/30-shoes.conf`에서 `FASHION_SHOE_MODEL_PATH`를 지정한다. Slurm 작업이 바뀌어도 적용된다.
- **릴리스 무결성:** 코드 파일 300개의 SHA-256을 `DEPLOY_MANIFEST.json`과 대조했다. ZIP으로 배포할 때 서비스 제어 스크립트의 실행 권한이 빠지는 문제를 찾아 `gpu_server/scripts/fitta_web_controller.sh`의 755 권한을 복구했다.

### 확인한 결과

- main 대상 로컬 테스트 481개, 운영 통합본 로컬 테스트 521개 통과.
- 서버에서 통합본 테스트 513개 통과·2개 건너뜀(Node 미설치), 마지막 요청 호환성 테스트 6개 추가 통과. JavaScript 검사는 로컬에서 통과했다.
- 별도 GPU의 8001 포트에서 실제 무신사 신발 3건의 분석·추천·합성·다운로드·캐시를 확인한 후 운영 8000 포트로 전환했다.
- 전환 후 운영 API에서도 신발 3건과 캐시 재요청이 통과했다(`SHOE_API_SMOKE_OK`). 새 상품은 기존 프롬프트 개발에 쓰던 두 상품과 다르다.
- 공개 `/api/health` 응답에서 CUDA, FASHN 파서, 학습된 의류 속성 헤드, `tryon_categories=["bottom", "shoes", "top"]`를 확인했다.
- 품질이 낮은 Simon 사진은 웹 입력 검사에서 차단됐다. 기준을 낮추지 않고 입력 검사를 통과한 `049713_0.jpg`로 웹 전체 경로를 검증했다. 생성기 직접 실행 성공과 웹 입력 적합성은 다른 검사다.

원본·결과·응답은 서버 `eval/shoes_vton_20260919/staging_api_valid/`, `production_api/`에 보관한다. 사진과 상품 이미지는 Git에 올리지 않는다. 발목 착용물 생성·상품 세부 디테일 한계와 실험 기능 안내는 유지한다.

### 남아 있는 공개 화면 작업

공개 홈페이지 `/`와 `app.js`는 별도 `192.168.0.110` 게이트웨이의 예전 파일이다. GPU 서버 API에는 신발이 반영됐지만 **기존 공개 화면에서 신발 선택 버튼을 사용할 수 있는 상태는 아니다**. 게이트웨이 접속 정보는 아직 확보되지 않았고 기존에 등록된 SSH 경로도 시간 초과됐다.

게이트웨이 담당자는 위 운영 통합본의 `web/static/`을 게이트웨이 서비스의 정적 파일 디렉터리에 반영해야 한다. 상의·하의·신발 선택, 성별 입력, 추천 근거, 사이즈 비교가 함께 들어 있으므로 app.js만 따로 교체하지 않는다. 게이트웨이 고유 API 주소 설정 `config.js`는 기존 값을 먼저 확인해 보존한다. 새 화면 적용 전까지는 구버전 `change_scope` 요청에 대한 서버 호환 처리로 기존 분석을 유지한다.

### 상태 확인과 복구

```bash
readlink -f /data1/dsl01/releases/fitta_current
systemctl --user status fitta-web.service
squeue -u dsl01
curl http://165.132.80.37:8000/api/health
```

이전 릴리스는 그대로 남아 있다. 운영 복구가 필요하면 링크를 되돌리고 서비스를 재시작한다. 재시작은 Slurm 재할당을 동반하므로 대기열에 들어갈 수 있다.

```bash
ln -sfn fitta_20260915_vton_quality_audit /data1/dsl01/releases/fitta_current
systemctl --user restart fitta-web.service
```
